{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "terminating-pod-reaper.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "terminating-pod-reaper.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
